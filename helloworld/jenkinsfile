pipeline {
    agent any
    stages { 
        stage('shwetha') {
            steps {
                    retry (3) {
                    sh '''
                    echo "This is my first command"
                    '''
                 sh '''
                  echo "Displaying my system uptime"
                  uptime
                  df -kh
                  date
                  '''
              
                 }
             } 
         }
     }
 }
